{
  "name": "resume-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o output.css --minify",
    "dev": "tailwindcss -i input.css -o output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
